{{/*
# SPDX-FileCopyrightText: Magenta ApS
#
# SPDX-License-Identifier: MPL-2.0
*/}}

OS2mo is deployed! :)

{{ if .Values.ingress.basic_auth }}
Produce basic auth credentials using:
```
kubectl get secrets/basic-auth-secret --template={{"{{"}}.data.username{{"}}"}} | base64 --decode
kubectl get secrets/basic-auth-secret --template={{"{{"}}.data.password{{"}}"}} | base64 --decode
```
{{ end }}
